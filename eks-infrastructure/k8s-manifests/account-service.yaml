apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service
  namespace: banking-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: account-service
  template:
    metadata:
      labels:
        app: account-service
    spec:
      containers:
      - name: account-service
        image: <ECR_ACCOUNT_ID>.dkr.ecr.<AWS_REGION>.amazonaws.com/banking/account-service:latest
        ports:
        - containerPort: 8081
        env:
        - name: DB_USERNAME
          value: "root"
        - name: DB_PASSWORD
          value: "password123"
        - name: DB_HOST
          value: "<RDS_ENDPOINT>"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: banking-system
spec:
  selector:
    app: account-service
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP